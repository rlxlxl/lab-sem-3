# Go Coverage Report - HTML Отчет о Покрытии Кода

Этот проект демонстрирует создание HTML-отчета о покрытии кода для Go-проекта с использованием библиотеки `testify`.

## Структура проекта

- `calculator.go` - основной код с функциями калькулятора и математических утилит
- `calculator_test.go` - тесты с использованием testify (assert, require, suite)
- `go.mod` - файл модуля Go с зависимостями
- `run_go_coverage.sh` - скрипт для генерации HTML-отчета

## Установка Go

### Быстрая установка (macOS с Homebrew)

Используйте готовый скрипт:
```bash
chmod +x install_go.sh
./install_go.sh
```

### Ручная установка

1. Проверьте, установлен ли Go:
   ```bash
   go version
   ```

2. Если Go не установлен:
   - **macOS**: `brew install go`
   - **Linux**: следуйте инструкциям на https://golang.org/doc/install
   - **Windows**: скачайте установщик с https://golang.org/dl/

3. После установки перезапустите терминал или выполните:
   ```bash
   source ~/.zshrc  # для zsh
   # или
   source ~/.bashrc  # для bash
   ```

## Установка зависимостей проекта

После установки Go установите зависимости проекта:
```bash
go mod tidy
```

## Запуск тестов

Запуск всех тестов:
```bash
go test -v ./...
```

Запуск тестов с покрытием:
```bash
go test -v -coverprofile=coverage.out ./...
```

## Генерация HTML-отчета о покрытии

### Автоматическая генерация (рекомендуется)

Используйте готовый скрипт:
```bash
./run_go_coverage.sh
```

Скрипт выполнит:
1. Запуск тестов с профилем покрытия
2. Генерацию HTML-отчета
3. Вывод статистики покрытия в консоль

### Ручная генерация

1. Создайте профиль покрытия:
   ```bash
   go test -coverprofile=go_coverage_report/coverage.out ./...
   ```

2. Просмотрите статистику в консоли:
   ```bash
   go tool cover -func=go_coverage_report/coverage.out
   ```

3. Сгенерируйте HTML-отчет:
   ```bash
   go tool cover -html=go_coverage_report/coverage.out -o go_coverage_report/coverage.html
   ```

4. Откройте отчет в браузере:
   ```bash
   open go_coverage_report/coverage.html  # macOS
   # или
   xdg-open go_coverage_report/coverage.html  # Linux
   ```

## Что показывает HTML-отчет

HTML-отчет предоставляет визуальный анализ покрытия кода:

- **Цветовая индикация**:
  - 🟢 Зеленый - код покрыт тестами
  - 🔴 Красный - код не покрыт тестами
  - 🟡 Желтый - частичное покрытие

- **Детальная информация**:
  - Процент покрытия для каждого файла
  - Процент покрытия для каждой функции
  - Подсветка строк кода с указанием покрытия

- **Навигация**:
  - Список всех файлов проекта
  - Переход к исходному коду с подсветкой

## Использование testify

Проект использует библиотеку `testify` для тестирования:

- `assert` - утверждения, которые не останавливают выполнение теста
- `require` - утверждения, которые останавливают выполнение теста при ошибке
- `suite` - структурированные наборы тестов

### Примеры использования

```go
// Простое утверждение
assert.Equal(t, expected, actual)

// Требование (останавливает тест при ошибке)
require.NoError(t, err)

// Использование suite
type MyTestSuite struct {
    suite.Suite
    calc *Calculator
}

func (suite *MyTestSuite) SetupTest() {
    suite.calc = NewCalculator()
}

func TestMySuite(t *testing.T) {
    suite.Run(t, new(MyTestSuite))
}
```

## Ожидаемый результат

После генерации отчета вы получите:

- Файл `go_coverage_report/coverage.html` - интерактивный HTML-отчет
- Файл `go_coverage_report/coverage.out` - профиль покрытия в текстовом формате
- Статистику покрытия в консоли с процентами для каждого файла и функции

## Пример вывода статистики

```
lab3-go/calculator.go:NewCalculator        100.0%
lab3-go/calculator.go:Calculator.Add       100.0%
lab3-go/calculator.go:Calculator.Subtract  100.0%
lab3-go/calculator.go:Calculator.Multiply  100.0%
lab3-go/calculator.go:Calculator.Divide    100.0%
lab3-go/calculator.go:Calculator.GetResult 100.0%
lab3-go/calculator.go:Calculator.Reset     100.0%
lab3-go/calculator.go:Calculator.Power     100.0%
lab3-go/calculator.go:Calculator.Abs       100.0%
lab3-go/calculator.go:MathUtils.Factorial  100.0%
lab3-go/calculator.go:MathUtils.IsPrime    100.0%
lab3-go/calculator.go:MathUtils.GCD        100.0%
total:                                      (statements)    95.0%
```

## Дополнительные возможности

### Фильтрация покрытия

Вы можете исключить определенные файлы из отчета:
```bash
go test -coverprofile=coverage.out -coverpkg=./... ./...
```

### Установка минимального порога покрытия

В CI/CD можно добавить проверку:
```bash
go test -coverprofile=coverage.out ./...
go tool cover -func=coverage.out | grep total | awk '{if ($3+0 < 80) exit 1}'
```

## Полезные ссылки

- [Go Testing Documentation](https://golang.org/pkg/testing/)
- [Testify Documentation](https://github.com/stretchr/testify)
- [Go Coverage Tool](https://golang.org/cmd/cover/)

